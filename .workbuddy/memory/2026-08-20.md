# 2026-08-20 工作记录

## 首页（Celeslime.github.io）改动
1. **留言板昵称输入框深色模式修复** (`css/main.css`)
   - 根因：深色下 `--blue-soft` 与 `--card` 同色 (#16242f)，`#gb-name` 填充与表单背景融成一片，输入框"消失"。
   - 修复：加 `html.dark .gb-form input#gb-name{ background: var(--blue-light); }`，并补 `::placeholder` 样式（var(--text) + opacity .45）。

2. **留言列表最新置顶** (`js/guestbook.js` → renderComments)
   - 渲染前按 `time` 降序排序（不依赖后端返回顺序），容错无效时间排末尾。

3. **删除"复制 QQ 号"功能** (`js/main.js`)
   - 移除 `qqBtn` 变量、`fallbackCopy` 函数、点击复制块、文件头 "+ QQ 复制" 注释。
   - **保留** `showTip`（被"情感"彩蛋复用）。`index.html` 的 QQ 跳转链接保留。

4. **右侧导航升级为 DeepSeek 风格 hover 展开卡片** (`index.html` + `css/main.css` + `js/main.js`)
   - 方案：折叠态保留 7 个小圆点 minimap（一眼看当前分区）；hover / 键盘聚焦 `.side-dots` 时，左侧浮出 `.side-dots-card` 条目卡片（width 190px 对齐 ds-virtual-list-width，含全部分区，点击跳转，active 高亮）。
   - HTML：圆点包进 `.side-dots-rail`，新增 `.side-dots-card` 条目列表（均为 `<a href="#x" data-target="x">`）。
   - CSS：卡片用 `visibility/opacity/pointer-events` 控制显隐（避免隐藏时还能 Tab 聚焦）；`right:100%` 贴右无间隙防 hover 闪烁；深浅模式走 CSS 变量。
   - JS：scrollspy 的 `dots` 选择器改为 `.side-dots-rail a`，新增 `cardLinks` 同步 `.active`。

## 环境与偏好
- 用户桌面在 **D:\Desktop**（不是 C:\Users\Cele\Desktop）。
- DeepSeek 快照 `D:\Desktop\IPX5 IPX4 IP55区别 - DeepSeek.html` 是对话页，导航代码在 minified 哈希类名组件里（含 `--scroll-nav-page-padding`），无可直接复用的 hover 卡片代码。

## 待办（来自 todo.txt，用户已排序）
- 后端智谱 API 违规报错 bug（pages-api，CF 上待网络稳定再做）—— 真实 bug，优先。
- 拆分后端 worker.js（上下文优化）。
- 跳过：SEO(#4，用户说不管)、换色系(#5)、音乐播放器(#6 风格不搭)、情绪新项目(#7)。
